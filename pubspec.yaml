name: ilkersevim_safe_parse
description: >-
  Safe parsing helpers for dynamic / JSON-like values (string, int, bool,
  map, list) without throwing on unexpected shapes.
version: 0.1.0
repository: https://github.com/redjadet/ilkersevim_safe_parse
homepage: https://github.com/redjadet/ilkersevim_safe_parse
issue_tracker: https://github.com/redjadet/ilkersevim_safe_parse/issues
topics:
  - json
  - parsing
  - dart

environment:
  sdk: '>=3.12.0 <4.0.0'

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0
